@model IEnumerable<Persona5APP.Models.Arcana>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Arcananame)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Arcananame)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.arcanaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.arcanaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.arcanaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
